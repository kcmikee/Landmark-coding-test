class ReservationItem{
    constructor(quantity, roomPrice,roomTitle,sum){
        this.quantity = quantity,
        this.roomPrice = roomPrice,
        this.roomTitle = roomTitle,
        this.sum = sum
    }
}

export default ReservationItem